<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "mybatis-3-mapper.dtd">

<mapper namespace="com.warehouse.mapper.ProduceMapper">
    <insert id="addProduceInfo" parameterType="com.warehouse.pojo.Produce">
        insert into produce(pr_id, pr_depart, pr_employee, inb_id)values(
        #{prId}, #{prDepart}, #{prEmployee}, #{inbId}
        )
    </insert>
</mapper>